package whg.WraithEngine.rendering;

public class SkinnedModel
// extends Model
{
	// public SkinnedModel(SkinnedMesh mesh, Location location, Material material)
	// {
	// super(mesh, location, material);
	// }
	//
	// @Override
	// public void render(Camera camera)
	// {
	// getMaterial().bind();
	// getMaterial().setMVPUniform(camera, getLocation());
	// ((SkinnedMesh) getMesh()).sendToShader(getMaterial().getShader());
	// getMesh().render();
	// }
}
